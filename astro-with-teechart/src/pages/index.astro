---
import { db, User } from 'astro:db';

const users = await db.select().from(User);
---

<h2>Users</h2>

{
 users.map(({ username, mail }) => (
    <article>
      <p>Username: {username}</p>
      <p>Mail: {mail}</p>
    </article>
  ))
}